DirectoryIndex index.php index.html

# No Loitering (Hide directory listings)
Options -Indexes

# Turbocharger (Gzip compression)
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript
</IfModule>

# --- BEGIN REWRITE LOGIC ---
RewriteEngine On

# The Master Anchor: Forces all logic to stay inside the /hearth1/ folder
RewriteBase /hearth1/

# COMMENTED OUT: Redirect to beta (Disabled for testing)
# RewriteRule ^(.*)$ /beta/$1 [R=301,L]

# Portable Re-piping (HTML to PHP)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.+)\.html$ $1.php [L,R=301]

# The "Manual U-Turn" (The 404 bypass)
# If the file doesn't exist AND the folder doesn't exist, serve 404.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ 404.php [L]

#